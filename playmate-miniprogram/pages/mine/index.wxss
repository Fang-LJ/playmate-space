.mine-page {
  padding-left: 48rpx;
  padding-right: 48rpx;
}

.user-card {
  display: flex;
  align-items: center;
  gap: 22rpx;
  padding: 30rpx;
}

.user-avatar {
  flex: 0 0 104rpx;
  width: 104rpx;
  height: 104rpx;
  border-radius: 28rpx;
  background: var(--pm-primary-light);
}

.user-avatar-placeholder {
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--pm-primary-dark);
  font-size: 36rpx;
  font-weight: 800;
}

.user-main {
  flex: 1;
  min-width: 0;
}

.user-name {
  color: var(--pm-text);
  font-size: 34rpx;
  font-weight: 800;
  line-height: 1.3;
}

.user-desc,
.user-phone {
  margin-top: 8rpx;
  color: var(--pm-text-muted);
  font-size: 24rpx;
  line-height: 1.45;
}

.user-phone {
  opacity: 0.82;
}

.user-badges {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 14rpx;
}

.mini-badge {
  padding: 7rpx 14rpx;
  border-radius: 999rpx;
  color: #8a6500;
  background: #fff7df;
  font-size: 21rpx;
  font-weight: 700;
}

.mini-badge.ok {
  color: var(--pm-primary-dark);
  background: var(--pm-primary-light);
}

.user-action {
  flex: 0 0 auto;
  color: var(--pm-primary-dark);
  font-size: 24rpx;
  font-weight: 700;
}

.mine-action-card,
.version-card,
.guest-card {
  padding: 30rpx;
}

.account-protection-card,
.profile-suggestion-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
  padding: 28rpx 30rpx;
}

.account-protection-card {
  border-color: #f0e0ae;
  background: #fffdf5;
}

.profile-suggestion-card {
  border-color: rgba(107, 175, 146, 0.24);
  background: #fbfefc;
}

.protection-action {
  flex: 0 0 auto;
  color: var(--pm-primary-dark);
  font-size: 24rpx;
  font-weight: 800;
}

.mine-action-card {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
}

.action-title {
  color: var(--pm-text);
  font-size: 31rpx;
  font-weight: 800;
  line-height: 1.35;
}

.action-desc {
  margin-top: 10rpx;
  color: var(--pm-text-muted);
  font-size: 25rpx;
  line-height: 1.45;
}

.action-arrow {
  color: #a4afa8;
  font-size: 40rpx;
  line-height: 1;
}

.logout-area {
  margin-top: 48rpx;
}

.logout-button {
  color: var(--pm-text-muted);
}

.login-actions {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}
